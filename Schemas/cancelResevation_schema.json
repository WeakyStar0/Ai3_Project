{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Cancel Reservation",
    "type": "object",
    "properties": {
      "reservationId": {
        "type": "string",
        "pattern": "^[a-fA-F0-9]{24}$"  // Exemplo para IDs de formato MongoDB (24 caracteres hexadecimais)
      }
    },
    "required": ["reservationId"]
  }
  